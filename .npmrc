# Strict peer dependency checking
strict-peer-dependencies=true

# Auto install peers (pnpm 9+ default)
auto-install-peers=true

# Use dedicated lockfile
lockfile-format=v6

# Hoist for compatibility with tools that expect node_modules layout
# (needed for @smithy/shared-ini-file-loader and other AWS SDK packages)
shamefully-hoist=true
